# Version-agnostic mod identity.
#
# This file is maintained on the `shared` branch and merged into every version
# branch, so a release bump is edited once rather than once per branch. Nothing
# here may reference a Minecraft version - those values live in gradle.properties,
# which is deliberately per-branch.
mod_version=1.1.3
maven_group=com.drinfonty.redfx

modrinth_project_id=uzc1jr0I
curseforge_project_id=1641217
